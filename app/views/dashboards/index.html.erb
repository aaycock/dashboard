<h1>Listing dashboards</h1>

<table>
  <tr>
    <th>Account</th>
    <th>Name</th>
    <th>Description</th>
  </tr>

<% @dashboards.each do |dashboard| %>
  <tr>
    <td><%=h dashboard.account_id %></td>
    <td><%=h dashboard.name %></td>
    <td><%=h dashboard.description %></td>
    <td><%= link_to 'Show', dashboard %></td>
    <td><%= link_to 'Edit', edit_dashboard_path(dashboard) %></td>
    <td><%= link_to 'Destroy', dashboard, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New dashboard', new_dashboard_path %>